# 🔧 دليل اختبار الاتصال - دار الإلهام

## ✅ الإصلاحات المطبقة

### 1. إصلاح إعدادات CORS
- تم تحديث `backend/server.js` للسماح بجميع المنافذ المحلية
- الآن يدعم: `localhost:5000`, `localhost:3000`, `localhost:8000`, `localhost:8080`
- في وضع التطوير، يسمح بجميع المنافذ

### 2. تحسين معالجة الأخطاء
- تم تحديث `frontend/assets/js/main.js` لمعالجة أفضل لأخطاء الاتصال
- رسائل خطأ أوضح عند فشل الاتصال

### 3. إزالة الخيارات المهملة
- تم إزالة `useNewUrlParser` و `useUnifiedTopology` من mongoose.connect

## 🧪 كيفية اختبار الاتصال

### 1. التحقق من Backend
```bash
# في Terminal
curl http://localhost:5000/api/health
```

يجب أن ترى:
```json
{"status":"ok","message":"دار الإلهام API يعمل بنجاح","timestamp":"..."}
```

### 2. اختبار صفحة Login

#### الطريقة 1: استخدام Live Server في VS Code
1. افتح VS Code
2. اضغط F1 واكتب "Live Server: Open with Live Server"
3. افتح `frontend/public/auth/login.html`
4. جرّب تسجيل الدخول

#### الطريقة 2: استخدام Python HTTP Server
```bash
cd frontend/public
python -m http.server 8000
```
ثم افتح: `http://localhost:8000/auth/login.html`

#### الطريقة 3: استخدام Node.js http-server
```bash
npx http-server frontend/public -p 8000
```
ثم افتح: `http://localhost:8000/auth/login.html`

### 3. التحقق من Console في المتصفح

1. افتح صفحة Login
2. اضغط F12 لفتح Developer Tools
3. اذهب إلى تبويب Console
4. جرّب تسجيل الدخول
5. راقب الرسائل في Console

### 4. إنشاء حساب جديد

إذا لم يكن لديك حساب:
1. افتح `frontend/public/auth/register.html`
2. املأ البيانات:
   - الاسم: (أي اسم)
   - البريد الإلكتروني: (بريد صحيح)
   - كلمة المرور: (8 أحرف على الأقل)
   - اختر Role: `parent` أو `student`
3. اضغط "سجل الآن"
4. بعد التسجيل، يمكنك تسجيل الدخول

## 🔍 حل المشاكل

### مشكلة: "FAILED TO FETCH"

**الحل:**
1. تأكد أن Backend يعمل:
   ```bash
   npm run dev:backend
   ```

2. تأكد أن MongoDB يعمل:
   ```bash
   # يجب أن يكون mongod مشغّلاً
   ```

3. افتح صفحة Login من Server محلي (ليس file://)

4. تحقق من Console في المتصفح لرؤية رسالة الخطأ الدقيقة

### مشكلة: "CORS Error"

**الحل:**
- تم إصلاحها! CORS الآن يسمح بجميع المنافذ المحلية
- إذا استمرت المشكلة، تأكد من فتح الصفحة من Server محلي

### مشكلة: "Cannot connect to server"

**الحل:**
1. تحقق من أن Backend يعمل على `http://localhost:5000`
2. جرّب فتح `http://localhost:5000/api/health` في المتصفح
3. تأكد من عدم وجود Firewall يمنع الاتصال

## 📝 ملاحظات مهمة

- **لا تفتح HTML مباشرة من الملف** (`file://`) - استخدم Server محلي
- **Backend يجب أن يعمل** قبل محاولة تسجيل الدخول
- **MongoDB يجب أن يعمل** قبل تشغيل Backend
- **استخدم Console في المتصفح** لرؤية رسائل الخطأ التفصيلية

## ✅ التحقق النهائي

قبل محاولة تسجيل الدخول، تأكد من:

- [ ] MongoDB يعمل (`mongod` مشغّل)
- [ ] Backend يعمل (`npm run dev:backend` يعمل)
- [ ] صفحة Login مفتوحة من Server محلي (ليس file://)
- [ ] Console في المتصفح مفتوح لرؤية الأخطاء

---

**تم التحديث:** 2026-01-11
**الحالة:** ✅ جاهز للاختبار
