# 📦 دليل تهيئة Git للمشروع

## ⚠️ ملاحظة مهمة

**Git يجب أن يكون مثبتاً أولاً!**

إذا لم يكن Git مثبتاً:
1. اذهب إلى: https://git-scm.com/download/win
2. حمّل وثبت Git
3. أعد فتح Terminal
4. ثم اتبع الخطوات أدناه

---

## 🚀 الخطوات السريعة

### الخطوة 1: التحقق من تثبيت Git

افتح Terminal واكتب:
```powershell
git --version
```

إذا ظهر رقم إصدار، Git مثبت ✅

---

### الخطوة 2: تهيئة Git في المشروع

#### الطريقة 1: استخدام السكربت (موصى به)

```powershell
.\setup-git.ps1
```

#### الطريقة 2: يدوياً

```powershell
# 1. تهيئة Git
git init

# 2. إضافة جميع الملفات
git add .

# 3. عمل Commit
git commit -m "Initial commit - Ready for deployment"
```

---

### الخطوة 3: إنشاء Repository على GitHub

1. اذهب إلى: https://github.com/new
2. أدخل اسم المشروع (مثال: `dar-al-ilham`)
3. اختر **Public** أو **Private**
4. **لا** تضع علامة على README
5. اضغط **"Create repository"**

---

### الخطوة 4: ربط المشروع بـ GitHub

بعد إنشاء Repository، ستظهر لك تعليمات. استخدم هذه الأوامر:

```powershell
# استبدل YOUR-USERNAME باسم المستخدم في GitHub
git remote add origin https://github.com/YOUR-USERNAME/dar-al-ilham.git

# تغيير اسم الفرع إلى main
git branch -M main

# رفع الكود
git push -u origin main
```

---

## 🔧 إعداد Git للمرة الأولى

إذا كانت هذه المرة الأولى لاستخدام Git:

```powershell
# إعداد اسم المستخدم
git config --global user.name "Your Name"

# إعداد البريد الإلكتروني
git config --global user.email "your.email@example.com"
```

---

## ✅ التحقق من الإعداد

بعد رفع الكود:
1. اذهب إلى: https://github.com/YOUR-USERNAME/dar-al-ilham
2. يجب أن ترى جميع ملفات المشروع

---

## 🆘 حل المشاكل

### المشكلة: "git is not recognized"

**الحل:** Git غير مثبت. قم بتثبيته من: https://git-scm.com/download/win

### المشكلة: "Permission denied"

**الحل:** 
- تأكد من تسجيل الدخول إلى GitHub
- استخدم Personal Access Token بدلاً من كلمة المرور

### المشكلة: "repository not found"

**الحل:**
- تأكد من اسم المستخدم في الرابط
- تأكد من إنشاء Repository على GitHub أولاً

---

## 📝 ملاحظات

1. **لا ترفع ملف `.env`** - يجب أن يكون في `.gitignore`
2. **استخدم commit messages واضحة** - مثل: "Initial commit"
3. **ارفع التغييرات بانتظام** - استخدم `git add .` ثم `git commit -m "message"`

---

## 🎯 الخطوات التالية

بعد رفع الكود إلى GitHub:
1. ✅ إعداد MongoDB Atlas
2. ✅ نشر على Render.com
3. ✅ ربط MongoDB بـ Render

راجع `DEPLOYMENT.md` للحصول على التفاصيل الكاملة.
