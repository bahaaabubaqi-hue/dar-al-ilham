# 🚀 دليل رفع موقع دار الإلهام على Render.com

هذا الدليل يوضح كيفية رفع موقع دار الإلهام على Render.com خطوة بخطوة.

---

## 📋 المتطلبات الأساسية

1. **حساب GitHub** (لرفع الكود)
2. **حساب MongoDB Atlas** (قاعدة البيانات السحابية - مجاني)
3. **حساب Render.com** (للسيرفر - مجاني)

---

## 🔧 الخطوة 1: إعداد MongoDB Atlas (قاعدة البيانات)

### 1.1 إنشاء حساب MongoDB Atlas

1. اذهب إلى: https://www.mongodb.com/cloud/atlas/register
2. أنشئ حساب جديد (مجاني)
3. اختر **Free Tier (M0)**

### 1.2 إنشاء Cluster

1. بعد تسجيل الدخول، اضغط **"Build a Database"**
2. اختر **FREE** (M0 Sandbox)
3. اختر **Cloud Provider** و **Region** (الأقرب لك)
4. اترك باقي الإعدادات كما هي
5. اضغط **"Create"**

### 1.3 إعداد قاعدة البيانات

1. انتظر حتى ينتهي إنشاء Cluster (2-3 دقائق)
2. اضغط **"Connect"**
3. اختر **"Connect your application"**
4. انسخ **Connection String** (سيبدو هكذا):
   ```
   mongodb+srv://<username>:<password>@cluster0.xxxxx.mongodb.net/?retryWrites=true&w=majority
   ```
5. **احفظ هذا الرابط** - ستحتاجه لاحقاً

### 1.4 إنشاء قاعدة البيانات

1. اضغط **"Database Access"** من القائمة الجانبية
2. اضغط **"Add New Database User"**
3. أدخل **Username** و **Password**
4. اختر **"Read and write to any database"**
5. اضغط **"Add User"**

### 1.5 السماح بالوصول من أي IP

1. اضغط **"Network Access"** من القائمة الجانبية
2. اضغط **"Add IP Address"**
3. اضغط **"Allow Access from Anywhere"** (0.0.0.0/0)
4. اضغط **"Confirm"**

---

## 📦 الخطوة 2: رفع الكود إلى GitHub

### 2.1 إنشاء Repository جديد

1. اذهب إلى: https://github.com/new
2. أدخل اسم المشروع (مثال: `dar-al-ilham`)
3. اختر **Public** أو **Private**
4. **لا** تضع علامة على README (لأن لدينا بالفعل)
5. اضغط **"Create repository"**

### 2.2 رفع الكود

افتح Terminal في مجلد المشروع:

```bash
# تهيئة Git (إذا لم يكن موجوداً)
git init

# إضافة جميع الملفات
git add .

# عمل Commit
git commit -m "Initial commit - Ready for deployment"

# إضافة Remote
git remote add origin https://github.com/YOUR-USERNAME/dar-al-ilham.git

# رفع الكود
git branch -M main
git push -u origin main
```

> ⚠️ **ملاحظة:** تأكد من عدم رفع ملف `.env` - يجب أن يكون في `.gitignore`

---

## 🌐 الخطوة 3: رفع الموقع على Render.com

### 3.1 إنشاء حساب Render

1. اذهب إلى: https://render.com
2. اضغط **"Get Started for Free"**
3. سجل باستخدام GitHub (موصى به)

### 3.2 إنشاء Web Service جديد

1. بعد تسجيل الدخول، اضغط **"New +"**
2. اختر **"Web Service"**
3. اختر **"Connect GitHub"** (إذا لم تكن متصلاً من قبل)
4. اختر المشروع **dar-al-ilham** من القائمة
5. اضغط **"Connect"**

### 3.3 إعدادات النشر

املأ البيانات التالية:

```
Name: dar-al-ilham-app
Environment: Node
Region: Singapore (أو أقرب region لك)
Branch: main
Root Directory: (اتركه فارغاً)
Runtime: Node
Build Command: npm install && npm run build:css
Start Command: npm start
```

### 3.4 إضافة Environment Variables

اضغط **"Advanced"** → **"Add Environment Variable"** وأضف:

| Key | Value |
|-----|-------|
| `NODE_ENV` | `production` |
| `PORT` | `5000` |
| `MONGODB_URI` | `mongodb+srv://username:password@cluster.mongodb.net/dar-al-ilham` |
| `JWT_SECRET` | `استخدم مفتاح عشوائي طويل (يمكن استخدام: openssl rand -base64 32)` |
| `FRONTEND_URL` | `اتركه فارغاً مؤقتاً` |

> ⚠️ **مهم:** استبدل `username` و `password` و `cluster` في `MONGODB_URI` بالقيم الحقيقية من MongoDB Atlas

### 3.5 النشر

1. اضغط **"Create Web Service"**
2. انتظر حتى ينتهي النشر (5-10 دقائق)
3. بعد انتهاء النشر، ستحصل على رابط مثل: `https://dar-al-ilham-app.onrender.com`

### 3.6 تحديث FRONTEND_URL

1. بعد الحصول على الرابط، اذهب إلى **"Environment"**
2. حدّث `FRONTEND_URL` إلى: `https://dar-al-ilham-app.onrender.com`
3. اضغط **"Save Changes"**
4. سيتم إعادة النشر تلقائياً

---

## ✅ الخطوة 4: التحقق من النشر

### 4.1 اختبار Health Check

افتح المتصفح واذهب إلى:
```
https://dar-al-ilham-app.onrender.com/api/health
```

يجب أن ترى:
```json
{
  "status": "ok",
  "message": "دار الإلهام API يعمل بنجاح",
  "timestamp": "..."
}
```

### 4.2 اختبار الصفحة الرئيسية

اذهب إلى:
```
https://dar-al-ilham-app.onrender.com/landing_page/index.html
```

يجب أن ترى الصفحة الرئيسية.

### 4.3 اختبار صفحة التسجيل

اذهب إلى:
```
https://dar-al-ilham-app.onrender.com/auth/register.html
```

---

## 🔧 حل المشاكل الشائعة

### المشكلة 1: MongoDB Connection Error

**الحل:**
- تأكد من صحة `MONGODB_URI`
- تأكد من السماح بالوصول من أي IP في MongoDB Atlas
- تأكد من صحة Username و Password

### المشكلة 2: Build Failed

**الحل:**
- تأكد من أن جميع `dependencies` موجودة في `package.json`
- تحقق من Logs في Render Dashboard
- تأكد من صحة `Build Command`

### المشكلة 3: Frontend لا يتصل بـ Backend

**الحل:**
- تأكد من تحديث `FRONTEND_URL` في Environment Variables
- تحقق من أن `main.js` يستخدم `baseURL` بشكل صحيح
- افتح Console في المتصفح وابحث عن أخطاء

### المشكلة 4: الموقع بطيء عند التحميل الأول

**الحل:**
- هذا طبيعي في الخطة المجانية من Render
- السيرفر "يذهب للنوم" بعد 15 دقيقة من عدم الاستخدام
- أول طلب بعد النوم يستغرق 30-60 ثانية

---

## 📝 ملاحظات مهمة

### الخطة المجانية من Render:

- ✅ **مجانية تماماً**
- ⏱️ **Auto-sleep:** السيرفر يذهب للنوم بعد 15 دقيقة
- 🚀 **Cold start:** أول طلب بعد النوم يستغرق 30-60 ثانية
- 📊 **RAM:** 512 MB
- 💾 **Storage:** 1 GB

### للاستخدام الإنتاجي (للعمل الفعلي):

- 🚀 ترقية إلى **Starter Plan** ($7/شهر)
- ⚡ **No auto-sleep** - السيرفر يعمل دائماً
- 📈 **512 MB RAM** + **10 GB Storage**

---

## 🔐 الأمان

1. **لا ترفع ملف `.env`** - تأكد من وجوده في `.gitignore`
2. **استخدم JWT_SECRET قوي** - لا تستخدم القيم الافتراضية
3. **احم MongoDB Atlas** - استخدم Password قوي
4. **فعّل Rate Limiting** - مفعّل تلقائياً في الكود

---

## 📞 الدعم

إذا واجهت أي مشكلة:
1. تحقق من **Logs** في Render Dashboard
2. تحقق من **MongoDB Atlas Logs**
3. افتح **Browser Console** وابحث عن أخطاء
4. تأكد من أن جميع **Environment Variables** موجودة وصحيحة

---

## 🎉 تهانينا!

الآن موقعك متاح على الإنترنت! 🚀

رابط الموقع: `https://dar-al-ilham-app.onrender.com`

---

**آخر تحديث:** يناير 2026
